apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-hr.fullname" . }}
  labels:
    {{- include "dclaw-hr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-hr.selectorLabels" . | nindent 4 }}
